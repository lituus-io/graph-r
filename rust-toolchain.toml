# Copyright: lituus-io, all rights reserved.
# Author: terekete <spicyzhug@gmail.com>
#
# Pinned to the same nightly channel as link-r: the bridge feature compiles
# link-r, whose GAT+TAIT async traits require `impl_trait_in_assoc_type`.

[toolchain]
channel = "nightly"
components = ["rustfmt", "clippy"]
